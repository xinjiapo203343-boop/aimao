# SG BizConnect 部署与使用说明

## 一、这个压缩包里有什么

```
sg-bizconnect/
├── index.html              ← 网站首页（正式上线用，会连接后端注册功能）
├── index-preview.html      ← 预览专用版本（双击可直接在浏览器打开看效果，不需要服务器）
├── config.php               ← 全局配置文件（管理员密码在这里改）
├── .htaccess                 ← Apache 安全配置（Nginx 服务器可忽略）
├── assets/
│   └── wechat-qr.png        ← 微信客服二维码图片
├── api/                      ← 后端接口（PHP），处理注册、登录、服务商申请、管理后台
│   ├── helpers.php
│   ├── register.php          ← 客户注册接口
│   ├── login.php              ← 客户登录接口
│   ├── provider.php           ← 服务商入驻申请接口
│   ├── admin_login.php
│   ├── admin_logout.php
│   ├── admin_guard.php
│   ├── admin_data.php
│   └── admin_action.php
├── admin/
│   └── index.html             ← 管理后台页面
├── data/                       ← 数据存储目录（自动生成 users.json / providers.json）
│   ├── .htaccess               ← 禁止外部直接访问数据文件（非常重要，不要删除）
│   ├── users.json
│   └── providers.json
└── 部署说明.md                  ← 就是你正在看的这份文件
```

## 二、服务器要求

- **必须支持 PHP 7.4 或以上版本**（大部分虚拟主机/云服务器默认都支持，宝塔面板、cPanel 都可以直接用）
- **不需要 MySQL 数据库**——本套系统用 JSON 文件保存数据，开箱即用，省去数据库配置的麻烦
- 如果用的是 Nginx 服务器，`.htaccess` 不会生效，需要请服务器提供商帮你在 Nginx 配置里禁止访问 `data/` 目录和 `config.php`（技术支持一般几分钟就能搞定）

## 三、上传到服务器的步骤（以宝塔面板/cPanel 为例）

1. **登录你的服务器管理面板**（宝塔、cPanel、或用 FTP 工具如 FileZilla）
2. 找到网站根目录，通常是 `/www/wwwroot/你的域名/` 或 `public_html/`
3. 把压缩包里 **除了 `index-preview.html` 之外的所有文件和文件夹**，原样上传到网站根目录
   （`index-preview.html` 只是给你自己预览用的，不需要放到服务器上，放了也没关系，只是没有用）
4. **设置文件夹权限**：在面板里找到 `data` 文件夹，右键设置权限为 `755` 或 `775`（如果注册功能报错，可以尝试改成 `777`，但正式环境不建议长期使用 777）
5. 确认网站已经绑定好你购买的域名，并且服务器已经安装 PHP 环境（面板里一般有"一键安装环境"或"PHP版本管理"）
6. 打开浏览器访问你的域名，应该就能看到网站首页了

## 四、修改管理员密码（务必第一时间做！）

打开 `config.php` 文件，找到这一行：

```php
define('ADMIN_PASSWORD_PLAIN', 'Admin@123456');
```

把引号里的 `Admin@123456` 改成你自己的强密码即可，比如：

```php
define('ADMIN_PASSWORD_PLAIN', 'MyStrongP@ssw0rd2026');
```

保存上传覆盖服务器上的 `config.php`，不需要做任何其他操作，系统会自动处理加密。

## 五、管理后台使用方法

- 访问地址：`https://你的域名/admin/`
- 默认账号：`admin`
- 默认密码：`Admin@123456`（**改密码后用你自己设置的新密码**）

登录后有 5 个标签页：

1. **客户注册**：查看所有通过官网注册的客户，可以停用/启用账号，或删除
2. **服务商审核**：查看所有申请入驻的服务商，点击"通过"或"拒绝"进行审核
3. **话题审核**：查看社区里所有发布的话题，可以"隐藏"（访客将看不到，但数据仍保留可恢复）或"删除"（彻底移除）
4. **首页内容管理**（新增）：不用改代码，直接在后台修改：
   - WhatsApp 客服号码
   - 页脚版权文字
   - 首页 4 个统计数字（认证服务商数量、服务企业数、覆盖国家数、托管交易额）
   - 首页 Top 10 服务商榜单（可编辑名称/专长描述/评分/评价数，可增加或删除整行）
   保存后刷新首页即可看到最新内容，无需重新上传任何文件
5. **系统诊断**（新增）：如果注册、申请提交后台看不到，先点这个标签页里的"重新检测"，会自动检查 PHP 版本、`data` 文件夹写入权限等，报错项会用红色标出，方便你快速定位问题再联系服务器提供商处理

> 建议：正式上线后，把后台地址 `/admin/` 改成一个不容易被猜到的路径（比如 `/admin-8x7k2/`），可以进一步防止被恶意扫描。改法很简单：把 `admin` 文件夹改个名字即可。

## 六、关于 WhatsApp 和微信客服按钮

- 网站右下角已经加了两个悬浮按钮：
  - **WhatsApp 按钮**：点击直接跳转到 `https://wa.me/33765114441`，是你提供的号码
  - **微信按钮**：点击弹出你提供的二维码图片（`assets/wechat-qr.png`），访客扫码即可添加
- 如果以后要更换二维码，直接把新图片重命名为 `wechat-qr.png` 替换 `assets/` 目录里的旧文件即可
- 如果要更换 WhatsApp 号码，打开 `index.html`，搜索 `wa.me/33765114441`，替换成新号码（格式：国家区号+号码，不要加 `+` 号和空格）

## 七、多语言说明

- 网站默认显示语言是**中文**
- 目前**中文、English、Bahasa Melayu（马来文）** 三种语言的内容是完整翻译好的，点击右上角语言按钮可以切换
- 页脚还列出了日语、韩语、越南语、印地语、阿拉伯语等入口，目前点击会提示"即将上线"——这是因为完整翻译几百条文案工作量较大，框架已经搭好，之后可以随时让我继续补充翻译内容
- 语言文案都集中写在 `index.html` 底部的 `<script>` 里的 `LANGS` 变量中，如果你自己或翻译人员想补充语言，可以直接在里面按现有格式添加

## 八、关于"预览版" index-preview.html

- 这个文件可以直接双击在浏览器里打开，不需要任何服务器环境，用来快速看效果
- 区别：预览版的"注册"、"登录"、"服务商申请"都是模拟效果（点击后直接提示成功，但不会真的保存数据），因为这些功能需要连接 PHP 后端，本地直接打开网页是无法访问服务器接口的
- 正式使用请以上传到服务器后的 `index.html` 为准，那个版本注册的数据是真实保存并能在后台看到的

## 九、重要提醒（关于页面中的"演示性"功能）

页面里原本设计的以下功能目前仍是**前端演示效果**，并没有连接真实的第三方系统，如果要正式使用需要额外对接（这类对接通常需要向对应平台申请商户资质、签约、开发对接，不是靠改代码就能实现的）：

- **托管支付 (Escrow)**：目前点击"支付"只是弹窗提示，并没有真实扣款。要实现真实收款托管，需要对接持牌支付网关或托管服务商（如 Stripe、PayNow API、或专业托管服务商），并且很可能涉及金融牌照/合规要求，建议先咨询新加坡金融监管相关的法律意见
- **AI 智能助手**：目前是预设关键词匹配的问答库，不是真实调用 AI 大模型。如果想接入真实 AI（比如接入 Claude 或其他大模型 API），我可以帮你继续开发，需要你提供对应的 API Key
- **社区发帖、二手市场发布**：目前发布内容只保存在浏览器当前页面里，刷新页面就会消失，还没有连接到后台数据库长期保存。如果需要，我可以继续扩展 `data/` 存储方案把这些也存起来

如果你确认要正式对外运营收款业务，强烈建议先咨询一位熟悉新加坡商业服务/支付合规的律师或顾问，这块内容涉及金融监管，我没有办法替你判断法律合规性。

## 十、常见问题排查

| 问题 | 可能原因 / 解决办法 |
|---|---|
| 注册按钮点击后没反应/提示"系统繁忙" | 检查 `data` 文件夹权限是否为 755/775；检查服务器 PHP 版本是否 ≥7.4 |
| 管理后台打不开或提示 401 | 清除浏览器 Cookie 后重新登录；确认 `admin_guard.php` 等文件是否完整上传 |
| 页面样式错乱、图标不显示 | 检查网络是否能访问 Google Fonts 和 Font Awesome 的 CDN（部分地区可能被墙，可以考虑把这些资源下载到自己服务器上） |
| 微信二维码不显示 | 确认 `assets/wechat-qr.png` 文件是否成功上传，路径大小写是否一致 |

## 十一、这次更新修复/新增了什么（如果你之前上传过旧版本）

如果你之前已经上传过一版到 `aimao88.com`，这次更新修复和新增了：

- **修复了一个关键 BUG**：之前"服务商审核"页面在部分服务器上打不开或看不到数据，原因是代码里用了 PHP 7.4 才支持的语法，而很多便宜虚拟主机默认还在用 PHP 7.0～7.2。已经改成兼容写法，实际上服务商申请数据一直都有正常保存，只是后台那一页显示不出来。
- **新增：话题审核**——现在社区发帖会真正保存到服务器（`data/posts.json`），后台可以查看、隐藏或删除
- **新增：首页内容管理**——WhatsApp 号码、页脚版权、统计数字、Top 10 榜单，都可以直接在后台改，不用再动代码
- **新增：系统诊断页**——遇到"后台看不到数据"这类问题时，可以先自查

**升级方法**：把压缩包里 `api/`、`admin/`、`index.html`、`data/content.json`（如果 `data` 目录下还没有这个文件）整个覆盖上传到服务器对应目录即可，`data/users.json` 和 `data/providers.json` 里已有的数据不会被覆盖丢失（不要删除这两个文件，直接跳过或覆盖上传其他文件即可）。

---

如果后续需要继续完善（比如接入真实支付、真实数据库、更多语言翻译、更专业的服务商审核流程等），随时可以告诉我，我可以在现有基础上继续扩展。
